# Generador_Oficios

Generador de oficios con membrete, tablas editables, columnas redimensionables y exportación a PDF/Word.

## Stack

- React + Vite
- html2canvas + jsPDF (PDF export)
- xlsx (Excel parsing)
